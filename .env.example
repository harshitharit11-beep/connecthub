# Frontend build variable (use the Render service URL in Netlify; `/api` is optional)
VITE_API_URL=http://localhost:3001/api

# Backend variables (Render and local API)
PORT=3001
FRONTEND_URL=http://localhost:5173
SESSION_SECRET=replace-with-a-long-random-secret
# Set this to a Render/PostgreSQL connection string in production.
# DATABASE_URL=